import { PlaceFilterPipe } from './place-filter.pipe';

describe('PlaceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
